@model Microsoft.AspNetCore.Identity.IdentityRole

@{
    ViewData["Title"] = "Delete Role";
}

<div class="content-wrapper">
    <!-- Content Header (Page header) -->
    <section class="content-header">
        <h1>
            Role
            <small> > &nbsp;@Html.DisplayFor(model => model.Name)</small>
        </h1>
        <ol class="breadcrumb">
            <li><a href="/"><i class="fa fa-dashboard"></i> Home</a></li>
            <li class="active">Role</li>
        </ol>
    </section>

    <!-- Main content -->
    <section class="content">
        <div class="row">
            <div class="col-md-12">
                <div class="box box-solid" style="box-shadow: none;">
                    <div class="box-header with-border" style="text-align: left;">
                        <i class="fa fa-text-width" style="margin-top: 20px"></i>
                        <h3 class="box-title" style="text-align: center">
                            Are you sure you want to delete this?
                        </h3>
                        <div style="float: right">
                            <input id="submitDelete" type="submit" form="formDelete" value="Delete" style="display: none" />
                            <a class="btn btn-app" onclick="$('#submitDelete').trigger('click');">
                                <i class="fa fa-trash"></i> Delete
                            </a>
                            <a class="btn btn-app" asp-action="Index">
                                <i class="fa fa-angle-left"></i> Back to List
                            </a>
                        </div>
                    </div><!-- /.box-header -->
                    <div class="box-body">
                        <dl class="dl-horizontal">
                            <dt>@Html.DisplayNameFor(model => model.Name)</dt>
                            <dd style="margin-bottom: 15px;">@Html.DisplayFor(model => model.Name)</dd>
                        </dl>
                    </div><!-- /.box-body -->
                    <form asp-action="Delete" id="formDelete" style="display: none">
                        <input type="hidden" asp-for="Id" />
                    </form>
                </div><!-- /.box -->
            </div><!-- ./col -->
        </div><!-- /.row -->
    </section><!-- /.content -->
</div><!-- /.content-wrapper -->